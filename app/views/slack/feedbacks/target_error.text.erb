Your message could not be sent because `<%= target %>` is not a correct user or a channel name. Maybe you mistyped it? Remember to prefix user names with `@` and channel names with `#` . If channel is private you need to invite bot so that he can post there.

Eg. `/abot #general i think that...`
